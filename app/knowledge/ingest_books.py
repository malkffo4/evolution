# Тебе нужно написать скрипт (например, app/knowledge/ingest_books.py), который пройдет по всему твоему дереву папок.

# Логика скрипта:
#     Берет файл (например, HTB Cereal.pdf).
#     Бьет его на чанки по 2000 токенов (с перекрытием контекста).
#     Шлет каждый чанк в Ollama с промптом выше.
#     Получает JSON с куском логики атаки.
#     Отправляет этот JSON по IPC (через твой app/runtime/ipc.py) в C-ядро.
#     C-ядро (в src/ingestion/perception.c) парсит JSON и делает upsert_edge в твою базу LMDB.
